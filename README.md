**OpenMRS** WebApplication

Requirement:-
**Java** : 11 +
**Maven**:

**No need** to download **chrome driver**.

open terminal and run command:-
**mvn clean test -Dsurefire.suiteXmlFiles=testng.xml**